# Courses — only the demo course is tracked (avoids commit of exam board material)
courses/*
!courses/how-bridges-work/

# Student data — private
# Symlink real student dirs from elsewhere, e.g.:
#   ln -s /path/to/tutor-data/students/Freda/gap-map-course-assesment students/freda
students/*
!students/example/

# Claude Code local settings
.claude/settings.local.json
